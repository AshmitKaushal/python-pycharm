# Functions are the block of reusable code that perform specific task.
# Functions are defined using def keyword followed by the function name paranthesis.

def gmean(a,b):
    return (a*b)/(a+b)


print(gmean(100,200))
print(gmean(10,20))
print(gmean(18,19))
